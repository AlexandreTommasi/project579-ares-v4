# CONFIGURAÇÕES DO BACKEND
NODE_ENV=development
PORT=3000
API_PREFIX=/api

# CONFIGURAÇÕES DO BANCO DE DADOS (SQL Server)
# Para conexão externa (ex: DBeaver), use 'localhost'. Dentro do Docker, o backend usará 'db_service'.
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=appdb
DB_USER=sa
SA_PASSWORD=yourStrong(!)Password # Senha do usuário 'sa' do SQL Server

# CONFIGURAÇÕES DE AUTENTICAÇÃO (JWT)
JWT_SECRET=a_very_secret_and_long_key_for_jwt_that_is_secure

# CONFIGURAÇÕES DO FRONTEND
# Esta variável é usada pelo frontend para saber onde a API está rodando.
# Em desenvolvimento com Docker, o frontend (no browser) acessa o backend via localhost.
VITE_API_URL=http://localhost:3000/api
